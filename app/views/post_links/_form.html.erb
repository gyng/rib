<%= form_for(@post_link) do |f| %>
  <% if @post_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post_link.errors.count, "error") %> prohibited this post_link from being saved:</h2>

      <ul>
      <% @post_link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_from %><br />
    <%= f.number_field :post_from %>
  </div>
  <div class="field">
    <%= f.label :post_to %><br />
    <%= f.number_field :post_to %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
